import { GoldenBootPage } from './GoldenBootPage';

// Awards hub — pre-tournament predictions. (Golden Boot today; Tournament Winner,
// Player of the Tournament and Dark Horse are added next.)
export function AwardsPage() {
  return (
    <div className="awards">
      <GoldenBootPage />
    </div>
  );
}
