apiVersion: v1
kind: Service
metadata:
  name: wc2026-web
  labels:
    {{- include "wc2026.labels" . | nindent 4 }}
spec:
  selector:
    app: wc2026-web
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.web.port }}
