apiVersion: v1
kind: Service
metadata:
  name: wc2026-api
  labels:
    {{- include "wc2026.labels" . | nindent 4 }}
spec:
  selector:
    app: wc2026-api
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.api.port }}
